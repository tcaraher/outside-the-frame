---
pagination:
    data: gallery
    size: 1
    alias: image
permalink: "/{{image.tag}}/{{ image.title | slug }}/"
---


<head>
    <link rel="stylesheet" href="https://use.typekit.net/euo1fyy.css">
    <link rel="stylesheet" href="/css/global.css">
</head>

<body>
    {% include "header.njk" %}
    <main>
        <div class="showcase__photo-block flex flex--col" >
                {% img
                src=image.src,
                alt=image.alt,
                className="showcase__img",
                sizes="(max-width: 480px) 90vw, (min-width: 640px) 90vw, (min-width:800px) 90vw,90vw"
                %}
                <div class="showcase__info">
                    <h1 class="photo-info__title">{{ image.title }}</h1>
                    <p class="photo-block__date">{{ image.date }}</p>
                    <p class=""> {{ image.location }} </p>
                </div>
{#                <div class="prev-next">#}
{#                    {% if pagination.href.previous %}#}
{#                    <a class="prev" href="{{ pagination.href.previous }}"><span class="lt">&lt;</span> Prev</a>#}
{#                    {% endif %}#}
{#                    {% if pagination.href.next %}#}
{#                    {% if not pagination.href.previous %}#}
{#                    <a class="next" style="margin-left: auto; margin-right: 0;" href="{{ pagination.href.next }}">Next#}
{#                        <span class="gt">&gt;</span></a>#}
{#                    {% else %}#}
{#                    <a class="next" href="{{ pagination.href.next }}">Next <span class="gt">&gt;</span></a>#}
{#                    {% endif %}#}
{#                    {% endif %}#}
{#                </div>#}
        </div>
    </main>
</body>

